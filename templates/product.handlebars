<script id="product" type="text/x-handlebars-template">
	<div class="product-container" data-campaignId={{campaignId}} data-productId={{productId}}>
		<div class="imageContainer">
			<img src="{{productImagePath}}" alt="{{productName}}" />
		</div>
		<div class="product-details">
    		<h3>{{productName}}</h3>
    		<p class="price">{{currencySymbol}}{{price}}</p>
	    </div>
		<div class="product-modifier">
			<button class="minus">-</button>
			<input type="textbox" data-productId="{{productId}}" value="{{quantity}}" disabled/>
			<button class="plus">+</button>
		</div>
		<div class="deleteContainer">
			<span class="delete">×</span>
		</div>
	</div>
</script>